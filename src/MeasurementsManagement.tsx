import { FormEvent,useEffect,useState } from 'react';
import { Link } from 'react-router-dom';
import { apiFetch } from './lib/api';
type Customer={id:string;customerNumber:string;firstName:string;lastName:string};type Type={id:string;code:string;name:string;unit?:string|null};type Row={id:string;customerId:string;customerName:string;customerLastName:string;value:string;measuredAt:string;notes?:string|null;typeId:string;typeName:string;unit?:string|null};
export default function MeasurementsManagement(){const[customers,setCustomers]=useState<Customer[]>([]),[types,setTypes]=useState<Type[]>([]),[rows,setRows]=useState<Row[]>([]);const[customerId,setCustomerId]=useState(''),[typeId,setTypeId]=useState(''),[value,setValue]=useState(''),[notes,setNotes]=useState(''),[measuredAt,setMeasuredAt]=useState('');const[error,setError]=useState(''),[message,setMessage]=useState(''),[saving,setSaving]=useState(false);
async function load(){try{const[c,t,r]=await Promise.all([apiFetch<{customers:Customer[]}>('/customers?limit=500'),apiFetch<{types:Type[]}>('/measurements/types'),apiFetch<{measurements:Row[]>}('/measurements'+(customerId?'?customerId='+encodeURIComponent(customerId):''))]);setCustomers(c.customers);setTypes(t.types);setRows(r.measurements);if(!customerId&&c.customers[0])setCustomerId(c.customers[0].id);if(!typeId&&t.types[0])setTypeId(t.types[0].id);}catch(e){setError(e instanceof Error?e.message:'تعذر تحميل القياسات');}}useEffect(()=>{void load()},[]);
async function save(e:FormEvent){e.preventDefault();setSaving(true);setError('');try{await apiFetch('/measurements',{method:'POST',body:JSON.stringify({customerId,measurementTypeId:typeId,value,measuredAt:measuredAt?new Date(measuredAt).toISOString():undefined,notes:notes||null})});setValue('');setNotes('');setMessage('تم حفظ القياس');await load();}catch(e){setError(e instanceof Error?e.message:'تعذر حفظ القياس')}finally{setSaving(false)}}async function remove(id:string){if(!window.confirm('حذف هذا القياس؟'))return;try{await apiFetch('/measurements/'+id,{method:'DELETE'});await load()}catch(e){setError(e instanceof Error?e.message:'تعذر حذف القياس')}}return <main className="app-shell"><header className="app-header"><div><span className="eyebrow">MEASUREMENTS</span><h1>القياسات الصحية</h1></div><Link className="secondary-button" to="/admin/dashboard">لوحة الإدارة</Link></header>{error&&<div className="info-strip warning">{error}</div>}{message&&<div className="info-strip">{message}</div>}<section className="panel"><div className="panel-heading-row"><div><p className="eyebrow">RECORD</p><h2>تسجيل قياس جديد</h2></div></div><form className="form-stack" onSubmit={save}><div className="form-row"><label>العميل<select required value={customerId} onChange={e=>setCustomerId(e.target.value)}>{customers.map(c=><option key={c.id} value={c.id}>{c.customerNumber} — {c.firstName} {c.lastName}</option>)}</select></label><label>نوع القياس<select required value={typeId} onChange={e=>setTypeId(e.target.value)}>{types.map(t=><option key={t.id} value={t.id}>{t.name}{t.unit?' ('+t.unit+')':''}</option>)}</select></label><label>القيمة<input type="number" step="0.01" required value={value} onChange={e=>setValue(e.target.value)}/></label><label>وقت القياس<input type="datetime-local" value={measuredAt} onChange={e=>setMeasuredAt(e.target.value)}/></label></div><label>ملاحظات<textarea value={notes} onChange={e=>setNotes(e.target.value)}/></label><button className="primary-action button" disabled={saving}>{saving?'جارٍ الحفظ...':'حفظ القياس'}</button></form></section><section className="panel"><div className="panel-heading-row"><div><p className="eyebrow">HISTORY</p><h2>سجل القياسات</h2></div><button className="secondary-button" onClick={()=>void load()}>تحديث</button></div><div className="staff-table-wrap"><table className="staff-table"><thead><tr><th>العميل</th><th>القياس</th><th>القيمة</th><th>الوحدة</th><th>التاريخ</th><th></th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.customerName} {r.customerLastName}</td><td>{r.typeName}</td><td>{r.value}</td><td>{r.unit??'—'}</td><td>{new Date(r.measuredAt).toLocaleString('ar-SA')}</td><td><button className="secondary-button" onClick={()=>void remove(r.id)}>حذف</button></td></tr>)}</tbody></table></div></section></main>}